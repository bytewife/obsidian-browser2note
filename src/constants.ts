import type { ExtensionLocalSettings } from './types'

export const NULL_FILENAME = ''
export const NULL_LINENUMBER = -1

export const DefaultLocalSettings: ExtensionLocalSettings = {
  version: '0.1',
  insecureMode: false,
  apiKey: '',
}

